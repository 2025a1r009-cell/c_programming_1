#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N,i;
    scanf("%d",&N);
    for(i = 1;i <= N; ++i){
        if(N % i == 0){
            printf("%d ", i);
        }
    }
    printf("\n");
    return 0;
}
