#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N,next,first = 0, second = 1,i;
    scanf("%d",&N);
    for (i=1;i<=N;i++){
        printf("%d",first);
        if(i<N){
            printf(" ");
        }
        next=first+second;
        first=second;
        second=next;
    }
    printf("\n");
    return 0;
}
